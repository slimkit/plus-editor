html
body {
  margin: 0;
  padding: 0;
  height: 100%;
}

.quill-editor-wrap {
  position: relative;
  height: 100%;

  .hidden-input {
    display: none;
  }

  .ql-toolbar {
    position: fixed;
    bottom: 0;
    z-index: 10;
    width: 100%;
    height: 42px;
    background-color: #fff;
  }

  .ql-container {
    overflow: auto;
    padding-bottom: 42px;
    min-height: 0;

    .ql-editor {
      height: 100%;
    }
  }
}

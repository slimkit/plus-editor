html
body {
  height: 100%
}

body {
  // background: green
}

.editor-wrap {
  display: flex
  flex-direction: column
  height: 100%

  .ql-toolbar {
    flex: none
  }

  .ql-container {
    flex: auto
    height: auto
  }
}
